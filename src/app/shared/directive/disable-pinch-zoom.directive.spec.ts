import { DisablePinchZoomDirective } from './disable-pinch-zoom.directive';

describe('DisablePinchZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new DisablePinchZoomDirective();
    expect(directive).toBeTruthy();
  });
});
